@model Spike.Web.Models.ContentDeliveryModel
@{
    ViewBag.Title = "ContentDelivery";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Scripts {

    <script src="~/Scripts/pages/razor.js"></script>

    <script>

        $().ready(function () {

            var baseUrl = "@string.Format("{0}://{1}{2}", Request.Url.Scheme, Request.Url.Authority, Url.Content("~"))";
            Project.page.links.getJsonData = baseUrl + "Razor/GetJsonData";
            Project.page.links.getHtmlData = baseUrl + "Razor/GetHtmlData";

            Project.page.viewModel = Project.page.createViewModel(
                @Html.Raw(Json.Encode(new {
                    Model.SectionTitle,
                    MethodList = Model.Methods })));

            ko.applyBindings(Project.page.viewModel);
        });

    </script>
}

<div>
    <h1>@Model.PageName</h1>
</div>


<div class="container">


    <div class="row">
        <div class="col-sm-offset-2 col-sm-8">
            <h2>Initial Server Data</h2>


        </div>
    </div>

    <div class="row">
        <div class="col-sm-offset-2 col-sm-8">
            <h2>Get JSon Data</h2>


        </div>
    </div>

    <div class="row">
        <div class="col-sm-offset-2 col-sm-8">
            <h2>Get HTML Data</h2>


        </div>
    </div>

</div>